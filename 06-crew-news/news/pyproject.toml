[project]
name = "news"
version = "0.1.0"
description = "news using crewAI"
authors = [{ name = "Your Name", email = "you@example.com" }]
requires-python = ">=3.10,<3.14"
dependencies = [
    "crewai[tools]==1.9.3"
]

[project.scripts]
news = "news.main:run"
run_crew = "news.main:run"
train = "news.main:train"
replay = "news.main:replay"
test = "news.main:test"
run_with_trigger = "news.main:run_with_trigger"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.crewai]
type = "crew"
